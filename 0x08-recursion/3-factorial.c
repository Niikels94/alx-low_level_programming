#include "main.h"

/**
 * factorial - gets factorial of a number
 * @n: integer
 *
 * Return: Nil
 */
int factorial(int n)
{
	if (k < 0)
		return (-1);
	if (k == 0)
		return (1);
	return (k * factorial(k - 1));
}
